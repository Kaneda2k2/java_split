for ( String ref : referencias.split(";") )
		{
		        if ( ref != null && !ref.isEmpty() )
		        {
		        	System.out.println(ref);
		            //aqui haces lo que quieras        
		        }

